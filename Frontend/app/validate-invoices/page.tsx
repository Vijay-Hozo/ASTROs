import React from "react";
import ValidateInvoicesClient from "@/components/validate-invoices/validate-invoices-client";

export const metadata = {
  title: "Validate Invoices",
};

export default function Page() {
  return <ValidateInvoicesClient />;
}
